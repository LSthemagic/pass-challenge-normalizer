import { omnibeesConfig } from './omnibees.config.js';
import { hotelBedsConfig } from './hotelbeds.config.js';

export const providerConfigs = {
    omnibees: omnibeesConfig,
    hotelbeds: hotelBedsConfig
};